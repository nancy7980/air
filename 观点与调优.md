# AI 工具 PM · 观点与调优能力（文字稿，先不动 PPT）

> 目标：社招产品经理面试，证明对 **AI PPT** 与 **AI 文档阅读** 两条线的理解 + 实操判断。
> 用法：这些是"可以当成你自己的判断讲出来"的观点。每条都给了「主流怎么说 → 我的判断 → 为什么 → 产品启示」，方便你内化后用自己的话表达。

---

## 0. 一句话标签（自我定位）

挑一个：

- **A**：「做过竞品实测、能拆 Agent 链路的 AI 工具 PM——聚焦 AI PPT 与 AI 文档阅读两条线。」
- **B**：「我关注 AI 工具'生成之后'的环节：评测、可控、可编辑——尤其是 AIPPT 与 AI 文档阅读。」
- **C**（更锋利）：「别人卷生成质量，我卷生成之后的'信任与调优'——做过 AIPPT/AI 文档的竞品评测与链路诊断。」

> 建议 A 打底（稳、信息全），B/C 当面试加深印象的口头版本。

---

## 1. 五个有立场的判断（差异化观点）

### 判断一：通用 vs 垂直，是个伪二元

- **主流怎么说**：要么"大模型通用助手会吃掉一切垂直工具"，要么"垂直工具更懂场景、活得更好"。
- **我的判断**：真正的分水岭不是"通用还是垂直"，而是 **谁掌握"分发入口" + 谁掌握"交付物的最后一公里"**。通用助手赢在入口和意图捕获，垂直工具赢在交付完成度。终局是 **"通用入口 + 可插拔的垂直交付内核"** 的组合。
- **为什么**：用户的需求是"动词"（我要做份汇报），不是"名词"（打开某个 app）。入口负责捕获动词，交付内核负责把它做完。两者分层，不是二选一。
- **产品启示**：对千问这种通用助手，关键 **不是自己亲手做多垂直，而是把 AIPPT、AI 文档阅读做成"能被通义入口调用的交付内核"**。AIPPT 不该是独立 app，而是通义下的一个输出能力。

### 判断二：Agent 自由度，不是越高越好，而要"分级授权"

- **主流怎么说**：Agent 越自主、越 end-to-end 越先进。
- **我的判断**：自由度应该 **按"可逆性 × 可验证性"分级授权**，而不是全程拉满。
  - 可逆 + 可验证的步骤（找资料、配图、排版草稿）→ 给高自由度，自动做。
  - 不可逆 / 高代价 / 硬约束的步骤（页数、品牌规范、对外发布）→ 收紧，强制硬校验或卡用户确认。
- **为什么**：用户对 Agent 的信任 = 它"该自主的地方自主、该收手的地方收手"。**豆包 11 页超标，本质就是在'硬约束'这种最该收手的地方，放任了自由度**——这不是模型不行，是自由度边界设计错了。
- **产品启示**：好的 AIPPT Agent 应该有一张 **"自由度地图"**：哪些环节自动跑、哪些环节必须回问/校验。这比单纯吹"全自动生成"更专业、更可信。

### 判断三：AIPPT 的核心，不是"内容生成"，是"约束满足 + 设计系统生成"

- **主流怎么说**：AIPPT = 让大模型把内容写出来、套个模板。
- **我的判断**：写字 LLM 早就会了，AIPPT 真正难的是四件工程化的事：
  1. **把抽象风格转成一致的设计系统**（"落日飞车"→ 夕阳/暖棕/复古相纸这一整套可执行视觉语言）；
  2. **满足硬约束**（页数/比例/语言/品牌）；
  3. **图文版式的工程化渲染**（代码排版，而非套模板）；
  4. **可编辑、可迭代**（不是一锤子出图）。
- **反共识**：AIPPT 的竞争力正在 **从"模型"转移到"渲染引擎 + 设计系统 + 质检闭环"** 这些工程能力。模型同质化后，谁的"代码排版 + 自检"强谁赢——这恰好踩中千问 **"代码能力领先"** 的优势。
- **"AIPPT 是伪需求吗？"**：不是伪需求，但 **"一次生成完美 PPT"是伪命题**。真实需求是"把我从 0→80 分的草稿快速搭好，我再改到 95"。所以 **可编辑性、可控性、迭代效率，比"一次惊艳"更重要**——这也是为什么千问"可编辑性略好"是被低估的优势。

### 判断四：AI 文档阅读的命门是"可信"，不是"读懂"

- **主流怎么说**：AI 文档阅读 = 总结 + 问答，比谁总结得全。
- **我的判断**：总结能力早已同质化。用户不敢用的真正原因是 **"不知道答案是不是编的"**。所以护城河在 **长文档/多文档的结构化理解 + 证据绑定（页码/段落可溯源）+ 冲突识别 + 主动拒答**。
- **为什么**：文档阅读多用于研报、合同、论文、经营汇报——这些场景 **一个幻觉就毁掉整次使用**。可信度 > 流畅度。
- **和 AIPPT 的协同（这条最值钱）**：文档阅读是"输入端理解"，AIPPT 是"输出端表达"。**千问若把"读完文档 → 直接生成汇报/PPT"打通，就是一条通用厂商才做得出的端到端链路**（钉钉"读完周报直接出汇报"、夸克"读完资料直接出答辩"）。这本身就是一个独到的产品主张。

### 判断五：行业在卷"生成"，真正的洼地是"生成之后"

- **主流怎么说**：大家都在比谁生成得更快、更好看。
- **我的判断**：被忽略的真问题全在"生成之后"：
  1. **可编辑与协作**——生成只是开始，改稿才是高频；
  2. **评估闭环缺失**——没人能量化"这次到底好不好、有没有越改越好"（所以我去搭了评测体系）；
  3. **信任与可控**——硬约束遵守、引用可溯源。
- **一句话**：**"生成"是入场券，"编辑、评估、信任"才是壁垒。** 这也是我整份评测的底层立场。

---

## 2. 出问题怎么调优（我的操作框架，不是教科书清单）

> 这套是"诊断顺序 + 调优杠杆优先级"，核心是两句话：
> **① 先把坏结果归因到链路某一环，别一上来就怪模型；② 永远先用最便宜、最可逆的杠杆，换模型是最后一招。**

### 第一步 · 归因：坏在哪一环？

AIPPT / AI 文档的链路：
**意图理解 → 内容规划 → 检索 / RAG → 工具调用 → 渲染排版 → 交付质检**

我的判定顺序（按"好判定 + 伤害大"排）：

| 顺序 | 先看什么 | 典型症状（用我手上的真实 case） | 大概率坏在哪一环 |
|---|---|---|---|
| 1 | **硬约束有没有被违反** | 豆包要求 1–10 页却出 11 页 | **交付质检 / 约束校验缺失**（≠ 模型不行） |
| 2 | **事实 / 拼写错误** | copiloto 拼错、"32K Token"过时 | **事实层 + 缺终检**（RAG/校验） |
| 3 | **结构是否对齐目标** | 跑题、没逻辑、为讲而讲 | **意图理解 / 内容规划** |
| 4 | **风格没落地 / 版式糊** | "落日飞车"没拆开、千问信息过载 | **规划深度** 或 **渲染 / 设计系统** |
| 5 | 以上都排除后 | 整体能力明显不够 | 才怀疑 **模型本身** |

> 关键判断：**前 4 类大多不是"模型不行"，而是"链路里少了一个环节或边界设错了"**。这正是 PM 能发力的地方——加环节、设边界，比等模型变强可控得多。

### 第二步 · 选杠杆：从最便宜、最可逆开始

| 优先级 | 调优杠杆 | 治什么 | 成本/可逆性 |
|---|---|---|---|
| 1 | **改指令 / 把软约束写成硬约束**（显式列 checklist） | 跑题、约束被忽略 | 最便宜、完全可逆 |
| 2 | **加 self-check / 二次校验环节**（生成后对照原始需求自查页数、风格、事实，不符就回炉） | 11 页、copiloto 这类"终检缺失" | 便宜、性价比最高 |
| 3 | **调 RAG**（检索策略 / 重排 / 引用绑定 / 拒答） | 无源数字、事实层、文档可信 | 中 |
| 4 | **喂 few-shot / 范例**（给它"好的设计系统长什么样"） | 风格落地、版式 | 中 |
| 5 | **改 workflow / 拆多 Agent**（规划-执行-质检分离） | 系统性、稳定性问题 | 较高 |
| 6 | **微调 / 换模型** | 真·能力不足 | 最贵、最不可逆，**最后手段** |

### 第三步 · 沉淀为回归用例（呼应评测体系）

- 每个 bug 沉淀成一个 **固定测试 case**，每次改 prompt / 换模型都回归跑一遍，防止"修好一个、坏三个"。
- **没有评测，调优就是盲调**——这就是我为什么先搭评测体系，再谈调优。

### 一个走完整流程的例子（可以当面试口播）

> 拿豆包"11 页"来说：我不会先去骂模型弱。我的判断是——这是 **交付质检环节缺失**。最便宜的修法是 **加一个"交付前对照原始需求"的 self-check**：让 Agent 生成后先数页数、比对风格清单、核对硬约束，不符合就自动回炉。这比"换一个更强的模型"便宜几个数量级，而且**可解释、可回归**。如果加了 self-check 还反复违反，我才会往上一层走——怀疑是规划环节没把"页数"当成硬约束，那就在规划阶段把它显式建模。

---

## 3. 给千问 AIPPT 的一条关键建议（备用，面试常被追问）

> 如果只提一条：**把"硬约束校验"做成产品级的显性能力**——生成前让用户确认硬约束清单（页数/比例/品牌/语言），生成后自动质检并展示"约束达成率"。
> 理由：硬约束是信任的开关；豆包恰恰栽在这里。这一条投入小、可感知、直接拉开和竞品的信任差距，也契合千问"代码能力强、适合做工程化校验"的优势。

---

## 4. 还需要你补的（只有你能提供）

1. **经历**：快手直播电商可以怎么用？（我建议的角度见下）你还有别的项目/实习吗？
   - 可用角度：直播电商一线 → 你**真实见过商家对"详情页/直播间素材"的内容生产诉求**：要快、要批量、要合规。这正好支撑"判断五：生成之后（批量、可编辑、合规）才是壁垒"和"AIPPT 落地到电商场景"的真实感。这是别的候选人没有的一手场景理解。
2. **图片**：原始 pptx / 高清截图，还是我改画"问题标注卡"？（你定方向我就处理）
3. 上面这些观点里，**哪几条你认同、想留**？哪条你不认同要砍？认同的我才往 PPT 里放。
